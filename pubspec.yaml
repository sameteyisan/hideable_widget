name: hideable_widget
description: Hideable Widget package helps to make any static widget hideable while scrolling.
version: 1.0.4
homepage: https://github.com/sameteyisan/hideable_widget.git

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: